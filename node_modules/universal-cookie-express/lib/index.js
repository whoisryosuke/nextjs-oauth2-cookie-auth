'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

exports.default = universalCookieMiddleware;

var _universalCookie = require('universal-cookie');

var _universalCookie2 = _interopRequireDefault(_universalCookie);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function universalCookieMiddleware() {
  return function (req, res, next) {
    req.universalCookies = new _universalCookie2.default(req.headers.cookie || '', {
      onSet: function onSet(name, value, options) {
        if (!res.cookie || res.headersSent) {
          return;
        }

        var expressOpt = _extends({}, options);
        if (expressOpt.maxAge) {
          // the standard for maxAge is seconds but express uses milliseconds
          expressOpt.maxAge = options.maxAge * 1000;
        }

        res.cookie(name, value, expressOpt);
      },
      onRemove: function onRemove(name, options) {
        if (!res.clearCookie || res.headersSent) {
          return;
        }

        res.clearCookie(name, options);
      }
    });
    next();
  };
}
module.exports = exports['default'];